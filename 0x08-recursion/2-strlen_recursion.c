#include <stdio.h>
#include <string.h>
#include <unistd.h>

/**
 * _strlen_recursion - function
 *
 * @s: char
 * Return: length
 */
int _strlen_recursion(char *s)
{
	int l;
	/**
	 * i - count
	 * l - length
	 */

	l = strlen(s);

	return (l);
}

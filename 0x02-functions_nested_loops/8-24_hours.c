#include <unistd.h>

/**
 * jack_bauer - function
 */
void jack_bauer(void)
{
	int n = '0', m, x, y;
	/**
	 * n - hour
	 * m - hour
	 * x - min
	 * y - min
	 */

	while (n < '3')
	{
		m = '0';

		while (m <= '9')
		{
			x = '0';

			while (x < '6')
			{
				y = '0';

				while (y <= '9')
				{
					return (nm:xy);
					write(1, "\n", 1);
					y++;
				}
				x++;
			}
			m++;
		}
		n++;
	}
}

#include "main.h"
#include <stdio.h>

int add(int, int);

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	int x, y, n;

	scanf("%d %d", &x, &y);
	n = x + y
	/**
	 * x - first number
	 * y - second number
	 * n - sum
	 */

	printf("%d\n", n);
	return (0);

}

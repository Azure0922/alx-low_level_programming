#include <string.h>
#include <unistd.h>

/**
 * puts_half - function
 *
 * @str: char
 * Return: nil
 */
void puts_half(char *str)
{
	int i, l = strlen(str);
	/**
	 *  i - count
	 *  l - length
	 *  m  mid
	 */

	for (i = 0; i > m; i++)
	{
		write(1, str, strlen(str) - (l / 2));
		write(1, "\n", 1);
	}
}

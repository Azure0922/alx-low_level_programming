#include <stdio.h>
#include <string.h>
#include <unistd.h>

/**
 * _strlen - function
 *
 * @s: char
 * Return: c
 */
int _strlen(char *s)
{
	int c;
	/* c - length */

	c = strlen(s);

	return (c);
}
